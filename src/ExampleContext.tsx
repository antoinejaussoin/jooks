import { createContext } from 'react';

export const Context1 = createContext({ foo: 'bar' });
export const Context2 = createContext({ ping: 'pong' });
export const Context3 = createContext({ ping: 'boom' });
